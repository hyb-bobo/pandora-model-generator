<?xml version="1.0"  encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.eohi.qll.writer.T700PriceBookWriter">

	<insert id="insert" parameterType="com.eohi.qll.model.T700PriceBook">
		insert into dbo.t_700_price_book (
			id,
			approval_status,
			note,
			price_method,
			supply_demand_relation_id,
			expire_date,
			purchase_price,
			effective_date,
            create_at,
			create_by,
			update_at,
			update_by,
			delete_at,
			delete_by,
			delete_flag,
			data_version
		) values (
            #{id},
			#{approvalStatus},
			#{note},
			#{priceMethod},
			#{supplyDemandRelationId},
			#{expireDate},
			#{purchasePrice},
			#{effectiveDate},
            #{createAt},
			#{createBy},
			#{updateAt},
			#{updateBy},
			#{deleteAt},
			#{deleteBy},
			0,
			1
		)
	</insert>

	<update id="update" parameterType="com.eohi.qll.model.T700PriceBook">
		update dbo.t_700_price_book set
        <if test="deleteFlag==0">
            approval_status = #{approvalStatus},
            note = #{note},
            price_method = #{priceMethod},
            supply_demand_relation_id = #{supplyDemandRelationId},
            expire_date = #{expireDate},
            purchase_price = #{purchasePrice},
            effective_date = #{effectiveDate},
            update_at = #{updateAt},
			update_by = #{updateBy},
        </if>
        <if test="deleteFlag==1">
            update_at = #{updateAt},
			update_by = #{updateBy},
			delete_at = #{deleteAt},
			delete_by = #{deleteBy},
			delete_flag = 1,
        </if>
        data_version = data_version + 1
		where
			id = #{id} and data_version = #{dataVersion}
	</update>


</mapper>